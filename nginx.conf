events {}

http {
    server {
        listen 7860;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }

        location /api/ {
            proxy_pass http://localhost:8080;
        }
    }
}
